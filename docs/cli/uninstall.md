---
summary: "CLI reference for .ravenox uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# .ravenox uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
ravenox uninstall
ravenox uninstall --all --yes
ravenox uninstall --dry-run
```
